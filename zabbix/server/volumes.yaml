apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: monitoring
  name: zabbix-server-pv-claim
spec:
  storageClassName: local
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi