apiVersion: v1
kind: Service
metadata:
  namespace: monitoring
  name: zabbix-web
spec:
  selector:
    app: zabbix-web
  ports:
  - name: web-http
    type: NodePort
    protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30080